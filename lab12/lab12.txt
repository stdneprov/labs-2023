#include <stdio.h>
#include <stdbool.h>

int main() {
    int num;
    while (scanf("%d", &num) == 1) {
        int multiple = 1;
        int new_num = 0;
        while (num != 0) {
            new_num += (9 - (num % 10)) * multiple;
            multiple *= 10;
            num /= 10;
            }
        printf("%d\n", new_num);
        }
    return 0;
}

